import Navbar from './Components/Navbar';
import Footer from './Components/Footer';


export default function Cart () {
    return ( <>
        <Navbar/>
        <h1>Sorry, this page is not found, hopefully like the problems in your GPU (:</h1>
        <Footer/>
    </> );
}